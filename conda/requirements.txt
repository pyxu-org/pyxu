numpy >= 1.19.*, < 2
scipy >= 1.4.*, < 2
dask >= 2.12.*, < 2022.12
sparse >= 0.13.*, < 1
numba >= 0.51.*, < 1

# GPU dependencies ------------------------------------------------------------
cupy >= 10.0.*, < 12
cudnn
cutensor
nccl
